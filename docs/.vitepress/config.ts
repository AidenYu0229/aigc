import { defineConfig } from 'vitepress'

export default defineConfig({
    title: ' ',
    themeConfig: {
        siteTitle: ' ',
        search: {
        provider: 'local'
        }
  }
})

themeConfig: {
    i18nRouting: true
}